# Consumer guide: using llama-mcp effectively

Moved to [`skills/llama-mcp-consumer/SKILL.md`](../skills/llama-mcp-consumer/SKILL.md) — packaged as an installable Claude skill (see README.md "Installing the consumer skill") so an orchestrating session picks it up automatically instead of relying on someone pasting this file. That's now the canonical source; this file is a pointer only, kept so old links don't 404.
